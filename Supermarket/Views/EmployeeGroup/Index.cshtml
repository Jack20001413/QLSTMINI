@using Application.DTOs
@model IEnumerable<EmployeeGroupDto>

@{ 
  ViewData["Title"] = "Index";
  ViewBag.Current = "EmployeeGroup";
}

<div id="view-all">
    @await Html.PartialAsync("_ViewAll", Model)
</div>

@section Scripts{
  
  @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    
}