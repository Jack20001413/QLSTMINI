@using Application.DTOs
@model IEnumerable<BrandDto>

@{ 
  ViewData["Title"] = "Index";
  ViewBag.Current = "Brand";
}

<div id="view-all">
    @await Html.PartialAsync("_ViewAll", Model)
</div>

@section Scripts{
  
  @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    
}